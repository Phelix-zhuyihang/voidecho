package com.example.voidecho.network;

/**
 * Reserved for future custom network packet implementations.
 * Currently no custom packets are needed; boss bar sync is handled
 * by vanilla's ServerBossBar and portal effects are purely client-side.
 */
public class ModNetwork {
    public static void init() {
        // No packets currently registered
    }
}
